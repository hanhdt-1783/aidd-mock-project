// Re-export canonical types from the lib layer so UI files have a stable, short import path.
export type {
  KudosUser,
  KudosCard,
  SidebarStats,
  GiftRecipient,
  SpotlightName,
  SpotlightActivity,
  KudosFilters,
  RecipientOption,
  CreateKudoInput,
} from '@/lib/kudos/types';
