# Kudos Responsive Polish Report
**Date:** 2026-05-30 (session ended 2026-05-31)
**Scope:** Phase 4 polish — additive responsive pass, no redesign

---

## Files Modified

### `app/globals.css` (+155 lines)
Added all `kudos-*` responsive helper classes and their breakpoint overrides.

### `app/_components/kudos/kudos-page.tsx`
- `kudos-sections-gap` on outer flex-column container (gap 120 → responsive)
- `kudos-feed-row` on `px-page` div wrapping feed + sidebar (flex-row → stacks on mobile)
- `kudos-section-heading` on All-Kudos `<h2>` (57px → scales down)
- `kudos-first-section-pad` on `div style={{paddingTop:64}}` before highlight section

### `app/_components/kudos/kudos-hero-banner.tsx`
- `kudos-hero-content` on the inner content `<div>` (existing class, was missing; adds 24px side padding at ≤1024px, reduces gap to 32px on mobile)
- `kudos-hero-eyebrow` on the title `<p>` (36px → scales down)
- `kudos-hero-logo-wrap` on logo container (104px height → scales down)
- `kudos-hero-actions` on the entry+search pill row (stacks on mobile)

### `app/_components/kudos/kudos-highlight-section.tsx`
- `kudos-highlight-header-row` on the title+filters row (flex-row → column on mobile)
- `kudos-section-heading` on `<h2>` (57px → scales down)

### `app/_components/kudos/kudos-highlight-carousel.tsx`
- `kudos-carousel-track` on the cards viewport div (overflow:hidden → overflow-x:auto scroll-snap on mobile)

### `app/_components/kudos/kudos-card-highlight.tsx`
- `kudos-card-highlight` on `<article>` (fixed 528px → fluid calc(100vw-48px) on mobile)

### `app/_components/kudos/kudos-spotlight-board.tsx`
- `kudos-section-heading` on `<h2>` (57px → scales down)
- `kudos-spotlight-canvas` on the canvas `<div>` (548px height → 420px tablet / 320px mobile)

### `app/_components/kudos/kudos-sidebar.tsx`
- `kudos-sidebar-wrapper` on `<aside>` (422px fixed → 320px tablet / 100% full-width + position:static on mobile)

### `app/_components/kudos/kudos-create-form.tsx`
- `kudos-form-label-col` on `LabelCell`'s `<div>` (160px fixed → 100% on mobile)
- `kudos-form-row` on recipient, hashtag, and image flex-row sections (stacks on mobile)
- `kudos-form-grid` on the "Danh hiệu" CSS grid section (2-col → 1-col on mobile)

---

## kudos-* Classes Added to globals.css

| Class | Base | Tablet ≤1023px | Mobile ≤767px |
|---|---|---|---|
| `.kudos-sections-gap` | gap:120px, pb:120px | gap:72px, pb:72px | gap:48px, pb:48px |
| `.kudos-first-section-pad` | — | pt:40px | pt:32px |
| `.kudos-feed-row` | flex-row, gap:80px | gap:40px | flex-column, gap:40px |
| `.kudos-sidebar-wrapper` | width:422px | width:320px | width:100%, position:static |
| `.kudos-highlight-header-row` | flex-row, justify:between | flex-wrap, gap:16px | flex-column, align:start, gap:12px |
| `.kudos-section-heading` | 57px/64px | 40px/48px | 32px/40px |
| `.kudos-hero-eyebrow` | 36px/44px | 28px/36px | 22px/30px |
| `.kudos-hero-logo-wrap` | height:104px | height:80px | height:60px |
| `.kudos-hero-actions` | — | — | flex-column, align:stretch, gap:16px |
| `.kudos-spotlight-canvas` | height:548px | height:420px | height:320px |
| `.kudos-carousel-track` | — | — | overflow-x:auto, scroll-snap:x mandatory |
| `.kudos-card-highlight` | width:528px | — | width:calc(100vw-48px), max-width:480px |
| `.kudos-form-label-col` | width:160px | — | width:100%, min-width:0 |
| `.kudos-form-row` | — | — | flex-direction:column, align:flex-start |
| `.kudos-form-grid` | — | — | grid-template-columns:1fr |

Also extended existing rule:
- `@media (max-width: 767px) .kudos-hero-content { gap: 32px !important }` — reduces 64px gap between title block and button row on mobile

All breakpoint overrides use `!important` to reliably override inline `style={{}}` attributes on the React elements.

Added `@media (prefers-reduced-motion: reduce)` block per polish-rules.md §3.

---

## Verification Results

| Check | Result |
|---|---|
| `npx tsc --noEmit` | 0 errors |
| `npx eslint app/_components/kudos app/globals.css` | 0 errors (1 non-code warning: CSS file has no ESLint config) |
| `npm run build` | ✓ Compiled successfully, 12 static pages generated |

---

## Desktop ≥1024px integrity

All responsive rules are scoped to `@media (max-width: 1023px)` or `@media (max-width: 767px)`. The base class declarations are identical to the existing inline styles (same values, just moved to CSS). Desktop at ≥1024px is visually unchanged.
